// create custome js it give error because it use jsx also first leter of function should be in capital letter
// {username} this is called evaluate expresion means final outcome
function Custom() {
    const username = "SK"
    return (
        <h1> Shaikh Arfat {username} </h1>
    )
}

export default Custom;